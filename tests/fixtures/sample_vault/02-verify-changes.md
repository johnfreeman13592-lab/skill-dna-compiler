# Verify changes

After changing code, run focused tests first.

Then run the full test suite and lint before reporting completion.

Synthetic redaction fixture only:

client_secret: "SYNTHETIC_SAMPLE_VALUE_123"
